html
	head
		title BKT-Wetten
		script(type="module", src="js/overviewMain.js")
	body
		section(id="profile")
			h1 Profil
			p(id="overviewProfileId")
			p(id="overviewProfileName") 
			p(id="overviewProfileRank") 
			p(id="overviewProfileMoney") 
			Button(id="refreshBetsButton") wetten neu laden
			p
			Button(id="logoutProfileButton") logout
			p
		section(id="activeBets")
			h1 Active Bets
